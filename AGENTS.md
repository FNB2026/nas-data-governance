# Engineering Guardrails

1. 默认只读；任何写文件能力必须进入独立执行层。
2. 扫描、分析、计划、审批、执行、校验、审计不得合并成一步。
3. 内容相同不等于可删除；必须同时判断目录职责与存储用途。
4. 不跟随符号链接、不跨挂载点、不越过任务根目录。
5. 保护规则优先于清理规则；冲突必须显式进入人工复核。
6. 外部 AI 默认关闭；敏感路径、文件名和内容不得出现在普通日志中。
7. 新增破坏性操作必须提供 stale 检查、隔离、校验、审计与回滚测试。
