# ADR-0002：去重计划保持建议性

状态：已接受

## 决策

计划生成器只能写出 `DRAFT` JSON 计划；即便完整哈希一致且目录角色为临时或缓存，也只建议 `QUARANTINE`，绝不触碰源文件。

## 原因

概念手册要求“内容重复”和“存储职责重复”同时成立，且扫描、分析、计划、审阅、执行、校验必须分离。当前目录角色仅来自路径信号，尚未包含业务锚点、工程依赖和人工审批。

## 后果

计划把敏感、原始、备份、系统目录立即升级为人工复核；目录角色不同同样进入复核。安全执行器只能在下一阶段接收已批准且执行前通过 stale 检查的计划。
